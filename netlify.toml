# Standard Next.js on Netlify (uses the Next runtime, not plain static `out/`).
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# For static export only (no plugin): remove the plugins block, set
#   command = "STATIC_EXPORT=1 npm run build"
#   publish = "out"
